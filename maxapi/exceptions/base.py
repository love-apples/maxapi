from __future__ import annotations


class MaxError(Exception):
    """Класс для всех исключений библиотеки maxapi.

    Позволяет ловить любые ошибки либы одним ``except MaxError``:
    сетевые сбои, ошибки API Макса, ошибки диспетчера, скачивания
    файлов и т.д. — всё наследуется от него.
    """
