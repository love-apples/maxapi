"""Базовое исключение библиотеки."""

from __future__ import annotations


class MaxError(Exception):
    """Базовый класс для всех исключений библиотеки maxapi.

    Позволяет ловить ошибки библиотеки одним ``except MaxError``:
    сетевые сбои, ошибки API Макса, ошибки диспетчера, скачивания
    файлов и т.д. — всё наследуется от него. Стандартные исключения
    Python в эту иерархию не входят.
    """
